# 问题记录索引

本目录保存当前问题、日志证据、实验修改和验收结果。已经完成对应范围实测的历史记录移入 `archive/`；归档文档可能包含已撤销代码、旧参数或当时尚未完成的计划，当前使用方式和有效实现仍以根目录 `README.md` 与 `docs/DEVELOPMENT.md` 为准。

## 当前状态摘要

- 当前可用传输：官方 Steam Lobby/Steam P2P；默认关闭的 `FRP Direct` 已通过公共 FRP UDP 三机基础联机及静态 `1` 人房满员提示实测。当前代码支持最多三台远端及地图内动态 `1` 至 `4` 人上限；四机、`2` 至 `4` 人容量边界和动态容量边界仍未实机验证。
- 当前分发构建的 `buildHash` 与 DLL SHA-256 以根目录 [README 当前状态](../README.md#当前状态) 为准。普通网络登记 Mook 的死亡事件与尸体终态同步已完成实现并部署；用户实测确认分叉概率明显降低，但仍有个别低概率不一致。晚加入死亡快照重放尚未实现。投掷物主动引爆与重复地形伤害已迁移到独立 issue。废弃文档中的“第七轮已部署”不再作为当前实现或验收依据。
- 已通过用户实测：异地高延迟加入、同一加入方不再生成 P2-P4 多个角色、联机 AFK 禁用开关、主机迁移后原房主重新加入的输入恢复、Workshop Esc 返回大厅和主菜单动画、官方 Steam 大厅与 FRP Direct 的彩色延迟名单及动态房主名、Steam 四人会话成员离场后名单样式保持、当前地图上的加入方正常退出/重入及房主 P1 恢复、Workshop 道具确定性和重复拾取防护、加入方缺少房主地图订阅时的自动识别、中文提示和加载阻止，以及加入方保留不同本地地图配置时在官方 Steam 大厅和 FRP Direct 中自动采用房主地图。
- 仍需排查：完成普通敌人死亡实体与尸体终态的 Steam/FRP、官方/Workshop 运行时验收，并继续实现晚加入死亡快照重放；钱币、金色奖励、Flex、载具等其它实体问题后续继续逐项建立样本和修复。AFK 后房主存活人数、异常断网重连和长期多轮退出/重入稳定性、Workshop ID `3781818421` 从第 3 关通关后进入第 4 关时黑屏、不同 Workshop 地图脚本兼容性仍需排查。`3715087178` 的关卡号逐帧递增根因已定位并加入保护，尚待通关复测。
- 尚待验收：`LEVEL_OUTCOME`、`WORKSHOP_GAME_MODE_COMPARE`、`OPTIONAL_BRO_MOD` 和新增 AFK 原生流程日志。
- 部分已验收：UMM 的 FRP 设置界面及自动应用已经用户实测；诊断日志分类过滤和预设保存行为仍待游戏内验收。
- 已通过基础双端实测、扩展场景待验收：FRP Esc 在线名单能够显示彩色 RTT 和动态渐变房主名；协议已升级到 v4，三/四机 RTT 快照、不同延迟档位和长名字布局仍需实机验收。
- 已通过四人会话实测、扩展场景待验收：官方 Steam 大厅 Esc 在线名单复用相同配色和房主动态渐变，延迟读取游戏原生 `PID.Ping`。房主加三名玩家的会话中，移除由滞后 `Room.PlayerCount` 触发的整份名单回退后，一名玩家离场时剩余名单能够保持彩色延迟和动态房主名。多人反复进出和长名字布局仍需实机验收。
- 已通过三机基础及静态 `1` 人房实测、扩展场景待验收：FRP Host 可用固定按钮设置 `1` 至 `4` 人房间上限；`1` 人房会阻止加入方并显示 5 秒满员提示。四机、`2` 至 `4` 人容量边界和降额后重入仍待专项实测。
- 已通过用户界面实测：FRP 只保留一个总开关；Host/Client 切换后自动应用对应配置，另一角色保存的端口或地址不会干扰当前角色，不再需要手动 Apply。

## 当前记录

| 文档 | 状态 | 阅读说明 |
| --- | --- | --- |
| `ISSUES-2026-08-28-全联机死亡实体与尸体终态同步.md` | 部分修复，分叉概率明显降低 | 已实现普通 Mook 的权威死亡事件和尸体终态提交；用户实测仍有个别低概率不一致，继续按 NID 收集残留样本。晚加入快照重放尚未实现。 |
| `ISSUES-2026-08-28-投掷物主动引爆与重复地形伤害.md` | 历史修复待重新确认 | 独立记录 `DemolitionBro`、`McBrover` 主动引爆同步和网络 Grenade 重复地形伤害；旧测试曾确认主动引爆恢复，但当前源码与构建需要重新确认。 |
| `ISSUES-2026-08-28-关闭Workshop注入后恢复官方地图.md` | 已修复并通过用户实测 | 记录关闭 Workshop Inject 后仍沿用 `Test Evan2` 的运行态残留、Steam 退出清理缺口、统一清理实现及官方地图恢复验收。 |
| `ISSUES-2026-08-27-第三方地图动态世界同步.md` | 已废弃，仅供历史参考 | 保留早期第三方地图动态世界同步的实验和历史观测，不再代表当前实现、分发状态或修复方案；死亡实体与尸体终态以后续独立 issue 为准。 |
| `ISSUES-2026-08-26-3715087178联机通关黑屏与关卡结束重入.md` | 已定位、实现并构建，尚待实机验证 | 双端连接正常；地图成功结束动作在切关期间反复清除原生完成保护，使关卡号逐帧递增。记录窄范围防重入补丁和复测判据。 |
| `ISSUES-2026-08-26-FRP多客户端与地图内动态房间人数上限.md` | 三机与静态 `1` 人房已验证，扩展范围待验收 | 记录最多三台远端、固定人数按钮、满员提示、地图内无重启调整容量、保留现有成员的实现和剩余验收矩阵。 |
| `ISSUES-2026-08-22-重复退出重入加入方失败与3781818421进入第4关黑屏.md` | 部分已定位并验证，仍有未解决范围 | 当前地图上正常退出后重入丢失房主 P1 的问题已修复并通过双端实测；旧构建约 4 轮后的加入失败、长期多轮稳定性和 `3781818421` 第 4 关黑屏仍需独立排查。 |
| `ISSUES-2026-08-25-Utility-Mod代码借鉴方案与AFK诊断改进.md` | 主要方案已实现，仍有待验收和保留项 | UMM 折叠分组、可勾选日志类别和针对性审查流程已实现；新增诊断、实际 UMM 界面及保存行为待验收，预序列化等候选改进仍保留。 |

## 已归档记录

以下记录已经完成各自范围内的用户实测，正文保留当时的日志、方案和构建信息，不再作为当前待办：

| 文档 | 归档状态 | 已验证范围 |
| --- | --- | --- |
| `archive/ISSUES-2026-08-19 联机问题记录与修复.md` | 已归档 | 早期联机修复、异地加入、晚加入及主机迁移相关流程已经后续实测。 |
| `archive/ISSUES-2026-08-20-角色退出重入后无法操作.md` | 已归档 | 角色退出、重新加入及输入恢复已经后续实测。 |
| `archive/ISSUES-2026-08-20-返回大厅和主菜单动画问题.md` | 已归档 | Workshop Esc 返回大厅及大厅返回主菜单动画已经实测。 |
| `archive/ISSUES-2026-08-24-联机加入方重复角色与AFK开关编译测试记录.md` | 已归档 | 重复角色修复、异地加入和 AFK 开关已经实测。 |
| `archive/ISSUES-2026-08-24-FRP内网穿透联机方案.md` | 已归档 | FRP Direct 公网双端基础游玩、Workshop 加载和玩家名单已经实测；文档中列出的扩展能力仍不因此自动视为完成。 |

## 阅读规则

1. 判断双方 DLL 是否一致时，优先比较运行日志中的 `BUILD_INFO buildHash`，不要只比较文件名、大小或修改时间。
2. “编译成功”不等于“联机测试通过”；只有明确记录实际测试结果的轮次才能作为验收依据。
3. 涉及网络根因时，应收集本次实际参与测试的所有端日志。异地端只能提供日志时，需要明确没有 MCP、UMM 日志或 `error.log` 的证据边界。
4. issue 中标明“改动恢复”“已撤销”或“未定位”的内容不得复制回当前实现，除非重新验证。
