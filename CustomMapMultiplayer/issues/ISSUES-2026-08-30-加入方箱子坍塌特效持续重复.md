# 加入方箱子坍塌特效持续重复

## 状态

| 项目 | 状态 |
| --- | --- |
| 加入方视觉异常 | 已复现 |
| 加入方日志错误循环 | 已确认 |
| 与 `BroforceBugFix` 爆炸箱递归问题的关系 | 调用链高度重合，根因未确认相同 |
| 当前源码修复 | 未实现 |
| 正式验收 | 未开始 |

本 issue 独立记录 Workshop 联机中加入方看到箱子特效持续重复、并伴随 `DoodadCrate` 错误循环的问题。它不能直接按 `BroforceBugFix` 中“相邻爆炸弹药箱递归栈溢出”已修复处理。

## 复现会话

- 日期：2026-08-30。
- 传输：官方 Steam Lobby/Steam P2P。
- Workshop ID：`3715087178`。
- 场景：`Test Evan2`。

## 用户可见现象

加入方发现一个箱子持续产生大量重复特效。加入方角色离开箱子一段距离后，特效减少, 靠近则持续增多；没有继续攻击该箱子也会继续出现, 双方随后正常退出房间并回到主菜单。

## 日志证据

加入方日志在当前会话中出现以下连续模式：

1. 场景初始化阶段出现 `DoodadCrate.SetupBlockAtStart` 的 `NullReferenceException`，并抑制了约 60 次重复错误。
2. 随后约每 5 秒出现一次 `DoodadCrate.DestroyBlockInternal` -> `Block.ActuallyCollapse` -> `DoodadCrate.ActuallyCollapse_Patch2` 调用栈。
3. 每轮抑制的重复错误约为 350 至 390 次，直到加入方退出房间仍在继续。
4. 退出房间后，加入方日志中的该错误循环停止。

当前证据没有看到加入方侧的 `CreateExplosion`、`EffectsDestroyed`、`StackOverflowException` 或 `Suppressed recursive DoodadCrate.ActuallyCollapse call` 记录。主机日志也没有出现同样的 `DoodadCrate` 调用栈。

## 日志清理后最小证据摘要

以下关键事实单独保留在 issue 中：

- 会话日期：2026-08-30；传输为官方 Steam Lobby/Steam P2P。
- Workshop ID：`3715087178`；场景：`Test Evan2`；本次实际关卡索引为 `0`。
- Host 会话 `auto-20260830-070120-157-a91aca5a`，Client 会话 `auto-20260830-070125-046-0aa3ccc0`；双方 `buildHash` 均为 `b00a97bae0f9eb52c7001d17f6c42a5077934f07684ee1bf3cab1bfb724e6a10`。
- 用户在加入方看到单个箱子大量重复特效；加入方角色离开箱子后特效仍持续，未继续攻击或拾取箱子。
- MCP 检查时加入方仍在线且存活，房主角色已死亡；双方仍处于同一场景、玩家数量为 2。
- 加入方先出现 `DoodadCrate.SetupBlockAtStart` 空引用错误，之后约每 5 秒重复出现 `DoodadCrate.DestroyBlockInternal` -> `Block.ActuallyCollapse` 调用链，每轮约抑制 350 至 390 条重复错误。
- 加入方退出房间后错误循环停止；双方最终均回到 `MainMenu`。
- 主机未出现同样的 `DoodadCrate` 调用栈；当前没有 `StackOverflowException` 或爆炸递归证据。

这段摘要用于保留已观察事实，不替代原始日志、截图或新的现场复现；删除原始日志后，后续根因分析的证据强度会相应降低。

## 与 `BroforceBugFix` 的关系

`BroforceBugFix` 的独立 issue 为 `DoodadCrate` 爆炸递归栈溢出，修复目标是 `DoodadCrate.ActuallyCollapse`：同一个箱子在尚未退出坍塌调用时再次进入该方法，则跳过这次嵌套调用。

两者的重合点：

- 都涉及 `DoodadCrate` 的坍塌处理；
- 都会表现为箱子特效异常和性能下降；
- 当前加入方调用栈中出现了 `DoodadCrate.ActuallyCollapse_Patch2`，说明该方法的 Harmony 包装路径确实被执行。

目前不能认定为同一个根因：

- `BroforceBugFix` 原 issue 的证据是相邻爆炸弹药箱通过 `CreateExplosion` 和 `EffectsDestroyed` 递归，最终触发 `StackOverflowException`；
- 本 issue 的证据是加入方在 `SetupBlockAtStart` 和 `DestroyBlockInternal` 阶段反复抛出空引用异常；
- 当前没有递归保护被触发的日志，也没有爆炸递归栈；
- 本次会话的关卡索引为 0，加入方还出现过 `gameState=NotSet` 与房间状态不一致，地图加载状态可能是干扰因素。

因此当前判断为：**同一类箱子坍塌调用链高度重合，但更像加入方箱体初始化/状态同步异常；是否需要 `BroforceBugFix` 的同一保护或额外修复，尚未确认。**

## 当前边界

- 本次现象只在一个官方 Steam Workshop 会话中记录，不能据此推断所有地图或所有箱子都会复现。
- 本次实际关卡为 `levelIndex=0`，不能替代原计划的第 7 关酸液测试。
- 尚未确认箱子是否为普通木箱、普通弹药箱或爆炸弹药箱。
- 尚未完成加入方与房主的同位置画面对照，也没有性能 p50/p95/p99 数据。
- 本次没有修改 `CustomMapMultiplayer` 或 `BroforceBugFix` 源码。

## 后续复测要求

1. 在不长时间停留的情况下，用同一张 Workshop 地图重新建立新会话，先确认实际关卡索引。
2. 记录箱子类型、出现位置和加入方/房主是否同时看到重复特效；不使用全场景对象枚举。
3. 保留两端完整诊断日志，重点对齐 `SetupBlockAtStart`、`ActuallyCollapse`、`DestroyBlockInternal` 和箱子相关错误的首次时间。
4. 分别观察不靠近箱子、靠近箱子、离开箱子和正常退出后的错误是否持续。
5. 如需验证 `BroforceBugFix` 的作用，应使用单独、可回退的受控会话；不能把临时运行时补丁或一次主观体感当作正式修复验收。

## 当前结论

本次已确认加入方存在可复现的箱子特效持续重复和 `DoodadCrate` 错误循环。该问题与 `BroforceBugFix` 的爆炸箱递归问题高度相关，但现有证据不足以证明是同一个根因，也不足以证明现有递归保护已经覆盖它。后续应先完成独立定位，再决定是在 `BroforceBugFix` 中扩展修复，还是在 `CustomMapMultiplayer` 中处理 Workshop 加载/同步条件。
