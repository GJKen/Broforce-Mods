# 海王三叉戟：投掷与近战

2026-09-15 最新素材修正：贴墙 10 帧、悬挂 18 帧、攀爬 20 帧已按 Rambro 对应格的动作像素重画，沿用当前 held-v2 外观。独立稿、总稿前 48 帧、身体图集与本地 PNG 已同步；本轮未重新构建或部署。下方构建记录属于此前接入版本，最新素材检查见 [rambro-refit-checks.json](traversal-v2/rambro-refit-checks.json)。

当前内容：三叉戟投掷、近战、新版站立、跑步、跳跃、落地、蹲持、蹲走，以及贴墙、悬挂、攀爬、爬梯和滑索。上述 v2 动作均已同步到身体、武器图集和取帧代码，最新一批已于 2026-09-14 23:41 构建部署并确认三项运行文件的哈希一致。验证与部署记录见 [反馈记录](haiwang_trident_反馈记录.md)。五类地形动作的游戏内表现尚待重启实测。

持戟跑步 v2（2026-09-14）共八帧，包含用户手调姿势及新的武器前倾节奏；完整兰博对照和预览同步保留。

同日新增持戟跳跃 v2：前进跳、原地跳及两套落地衔接，共 18 帧，只提供海王的分层 Aseprite 工程。当前游戏使用上述两份源稿及新版站姿。

同日新增 `haiwang_trident_crouch-v2.aseprite`：一帧蹲持和八帧蹲走，仅含海王的六个可编辑图层；已接入身体格 6、40–47 与武器格 64–72。

用户随后微调了本目录的 `haiwang_trident_held-v2.aseprite` 和 `haiwang_trident_crouch-v2.aseprite`。当前站姿以根目录新保存的 held-v2 为主稿，同时同步跳跃第 18 帧；蹲姿保留六个主体图层和新增握点修补层，按当前可见像素及图层顺序导入。取帧格号和游戏播放时序沿用现有代码。

本轮新增 `haiwang_trident_traversal-v2.aseprite`，32×32、86 帧，仅含海王六个可编辑图层，分为五个中文动作标签。外观采用本目录最新 held-v2，并查看当前跑步 v2/v3、跳跃和蹲姿源稿；上述五份已有源文件均保持原内容与 SHA-256。`archive` 的同名站姿仅作旧参考。

## 操作与数值

主攻击采用 Predabro 的松手投掷思路，近战键使用独立突刺。角色继续继承 CustomHero，以 Rambro 作为移动和身体动画基础。

| 操作 | 行为 |
| --- | --- |
| 轻按攻击键后松开 | 后拉 0.05 秒后掷出普通三叉戟 |
| 按住攻击键至少 0.5 秒后松开 | 出现青色水光，掷出强化三叉戟 |
| 近战键 | 前方约 24 单位突刺，0.08 秒命中，0.25 秒结束 |
| 特技键 | 奔涌水墙，保留 4 次特技弹药 |

| 参数 | 普通投掷 | 蓄力投掷 |
| --- | --- | --- |
| 速度 | 400 单位/秒 | 660 单位/秒 |
| 离手后的射程上限 | 180 单位 | 240 单位 |
| 基础伤害 | 6 | 10 |
| 普通敌人命中上限 | 2 个 | 4 个 |

近战对单位基础伤害为 8，对地形基础伤害为 4。伤害继续经过游戏的伤害修正和联机伤害接口。以上数值是第一版实装参数，尚未完成游戏内平衡验证。

松手后的投掷与收招共 0.28 秒，结束时以水光恢复持戟，无须拾取，不消耗特技弹药。收招期间的攻击输入不缓存，需要重新按下。受伤、眩晕、冻结、死亡、近战和特技会取消未完成的蓄力或出手，取消后松手不会补发一支戟。

## 命中与动画

- 每支戟记录已经处理过的单位，同一单位只结算一次命中；厚重敌人和 Boss 会挡停三叉戟。
- 飞行每隔至多 3 单位检查敌人，避免高速蓄力戟越过小体型敌人。
- 从角色到离手点也检查遮挡，贴墙出手不能直接把戟生成在墙后。
- 投掷和突刺均检查墙体与屏障；命中墙体或到达射程后，飞行戟以水光消散。
- 武器动作包含持戟、抬手、蓄力、蓄满、离手、收手、突刺准备、突刺、突刺收手。
- 投掷在离手帧生成弹体；突刺在有效帧结算伤害，手臂与戟尖一起向前伸出。

## 已接入游戏的身体与武器贴图

使用 Aseprite MCP 直接绘制武器、手臂和水光，并通过 Aseprite 原生拼版导出。保留已确认的金发、橙金鳞甲和绿裤靴配色，无抗锯齿。

新版站立、八帧跑步、十八帧跳跃与落地、九帧蹲姿与蹲走及八十六帧地形动作稿已经接入，以下记录当前游戏格号。地形动作的抓附手臂留在身体层，自由手臂与三叉戟在武器层，攻击切换时替换整条自由手臂。

| 身体图集格号 | 当前内容 |
| --- | --- |
| 0 | 最新 held-v2 站姿主稿的身体部分，与跳跃稿第 18 帧一致 |
| 6 | 蹲姿 v2 第 1 帧的蹲持身体 |
| 32–39 | 持戟跑步 v2 的八帧身体，80 毫秒/帧 |
| 40–47 | 蹲姿 v2 第 2–9 帧的八帧蹲走，游戏沿用原版 25 毫秒帧间隔 |
| 48–50 | 前进落地，接触、缓冲、恢复跑步，60/80/100 毫秒 |
| 64–69 | 前进跳跃，上升三帧、下落三帧 |
| 70–75 | 原地跳跃，上升三帧、下落三帧 |
| 76–95 | 新版贴墙、刀攀与预抓附；两组交替抓握阶段由原版 `knifeHand` 与高度选取 |
| 96–103 | 冲刺复用跑步 v2 的八帧身体 |
| 104–106 | 原地落地，接触、缓冲、恢复站立，60/80/100 毫秒 |
| 107–124 | 悬挂横移十二帧、停驻收势六帧 |
| 160–173 | 爬梯上行八帧、下滑三帧、停驻三帧 |
| 192–197 | 进出梯六帧过渡，按原版计数正向／反向播放 |
| 512–529 | 独立滑索十二帧逆行换手、六帧滑行收势 |
| 其余格号 | 继续使用项目原有身体动作 |

原资料中的 40–47 实际用于新版蹲姿移动。原有站跑跳落地和蹲姿共 44 个身体格继续保留；本轮新增接管 76 个地形动作格，其中 58 格位于原图集、18 个滑索格放在扩展行。图集格号按本机原版动画方法核对。

| 文件 | 用途 |
| --- | --- |
| haiwang_trident_held-v2.aseprite | 用户最新微调的独立站姿主稿，同步到站立与落地接站立末帧 |
| haiwang_trident_actions.aseprite | 32×32，36 帧；身体参考、三叉戟、手臂、水光分层 |
| haiwang_trident_gun_atlas.aseprite | 可编辑的游戏武器图集 |
| haiwang_trident_body.aseprite | 早期站立与跑步身体配合稿，共 9 帧 |
| haiwang_trident_run-v2.aseprite | 当前八帧跑步的分层源稿 |
| haiwang_trident_jump-v2.aseprite | 当前前进跳、原地跳和落地衔接的十八帧源稿 |
| haiwang_trident_crouch-v2.aseprite | 已接入游戏的新蹲持与蹲走九帧源稿 |
| haiwang_trident_traversal-v2.aseprite | 已接入游戏的贴墙、悬挂、攀爬、爬梯、滑索八十六帧源稿 |
| haiwang_trident_body_atlas.aseprite | 游戏身体图集，附隐藏的接入前参考 |
| haiwang_trident_projectile.aseprite | 普通与蓄力飞行戟，共 2 帧 |
| haiwang_trident_main_actions.png | 站立与跑步的武器姿态对照 |
| haiwang_trident_throw_preview.gif | 蓄力投掷预览 |
| haiwang_trident_thrust_preview.gif | 近战突刺预览 |
| haiwang_trident_run_preview.gif | 持戟跑步预览 |

原始 haiwang_standing.aseprite、haiwang_run.aseprite 保留在本目录；之前确认的静态武器设计稿与预览合成工程已移入 [archive](archive/)。当前文件用途见 [README.md](README.md)。

## 新版持戟跑步 v2（动画稿）

跑步 v2 初稿的外观参考保存在 `archive/haiwang_trident_held-v2.aseprite`；当前站姿和新增动作以本目录最新 held-v2 为准。主工程 `haiwang_trident_run-v2.aseprite` 共 8 帧，每帧 32×32、80 毫秒，`run` 标签按顺序循环，一轮 640 毫秒。身体姿势采用用户在完整对照工程中微调并同步回主工程的版本。

这次跑步武器节奏调整仅涉及 `金色三叉戟` 和 `持戟手臂`，保留用户微调的头发、脸部、鳞甲、双腿和后侧手臂。持戟手向身前收低，前臂连接随握点变化，肩部保留身体起伏；后侧手臂继续贴身小幅摆动。三叉戟随跨步前倾回摆，手掌始终盖住戟杆。三个戟尖完整落在画布内，戟尾收短 1 像素并保留尾饰，保持在脚底基准以内。

下表是当前动画稿的绘制参数。前倾角以直立为 0°，朝角色前进方向倾斜；握点坐标以画布左上角为 `(0, 0)`，向右、向下递增，半像素数值表示绘制时采用的握点中心。工程帧号从 1 开始，原版图集格号从 0 开始。

| 主工程帧 | 完整对照中的海王帧 | 兰博身体及武器格号 | 前倾角 | 握点 `(x, y)` |
| --- | --- | --- | --- | --- |
| 1 | 2 | 32 | 25° | (19.5, 22.5) |
| 2 | 4 | 33 | 27° | (19.5, 22.5) |
| 3 | 6 | 34 | 29° | (18.5, 22.5) |
| 4 | 8 | 35 | 30° | (18.5, 22.5) |
| 5 | 10 | 36 | 29° | (18.5, 22.5) |
| 6 | 12 | 37 | 27° | (18.5, 22.5) |
| 7 | 14 | 38 | 25° | (19.5, 21.5) |
| 8 | 16 | 39 | 24° | (19.5, 21.5) |

`haiwang_trident_run-v2_compare_rambro_full.aseprite` 共 16 帧，按“兰博 1、海王 1、兰博 2、海王 2……”交错，每帧 80 毫秒。奇数帧使用官方 `RAMBO_anim_1024x512.png` 的身体与 `RAMBRO_gun_anim_1024x64.png` 的持枪手臂和枪，两层均上移 1 像素，脚底基准为 `y=29`，参考图层锁定。旧 `haiwang_trident_run-v2_compare_rambro.aseprite` 使用不完整的蹲姿移动参考，不再作为普通跑步对照。

后续微调以用户最近编辑的海王帧为准。在完整对照中编辑偶数帧后，按上表同步回主工程，保持海王图层、像素、位置、透明度和时长一致；主工程修改后也同步至对照偶数帧，保留兰博奇数帧。再导出 `haiwang_trident_run-v2_sheet.png`（256×32）及配套 JSON、`haiwang_trident_run-v2_preview.gif`、`haiwang_trident_run-v2_compare_preview.gif` 和 `haiwang_trident_run-v2_frames.png`。并排 GIF 左侧为兰博、右侧为海王，同时播放对应的八帧，一轮 640 毫秒。

本轮素材检查已确认主工程与对照偶数帧一致、兰博参考及用户的其余身体图层保留、握点连续，海王合成帧无半透明像素。前一版修改前备份位于 `archive/before-run-dynamic-trident-20260914-152306/`。跑步 v2 已随站立、跳跃和落地动作接入并完成构建部署；游戏内表现待用户实测。

## 新版持戟跳跃 v2（动画稿）

`haiwang_trident_jump-v2.aseprite` 为 32×32、18 帧，包含 `绿色裤靴`、`金色鳞甲与颈部`、`后侧手臂`、`金发与脸部`、`金色三叉戟`、`持戟手臂` 六个可编辑图层，仅含海王。原版动作依据本机 `TestVanDammeAnim.AnimateActualJumpingFrames()` 核对，前进与原地跳跃分开处理；最高点复用下落身体。

| 工程帧 | 标签 | 原版身体参考格号 | 每帧时长 |
| --- | --- | --- | --- |
| 1–3 | 前进·上升 | 64–66 | 67 毫秒 |
| 4–6 | 前进·下落 | 67–69 | 67 毫秒 |
| 7–9 | 落地·接跑步 | 海王新增衔接 | 60、80、100 毫秒 |
| 10–12 | 原地·上升 | 70–72 | 67 毫秒 |
| 13–15 | 原地·下落 | 73–75 | 67 毫秒 |
| 16–18 | 落地·接站立 | 海王新增衔接 | 60、80、100 毫秒 |

上升与下落参考兰博的收腿、展腿姿态，在 Aseprite 中绘制海王裤靴，沿用用户的新版头部、鳞甲和后侧手臂。三叉戟和握点随阶段调整，发尾有轻微跟随；落地分接触、缓冲、恢复三个姿势。第 9 帧与当前 `haiwang_trident_run-v2.aseprite` 首帧像素一致，第 18 帧现已同步本目录 `haiwang_trident_held-v2.aseprite` 的最新用户站姿。初稿曾补实旧站姿手掌的一个半透明像素；当前末帧按最新源稿像素导出，保留六个主体图层，第 1–17 帧保持原样。

新增落地帧已由 `Land()` 和 `UpdateLandingAnimation()` 接入。原版完成落地物理、声音与特效后，再播放 0.24 秒的视觉衔接；攻击、再次起跳、蹲下、切换移动状态等会取消衔接。高处落地触发的原版翻滚继续优先播放。自然播完时从跑步首帧或站姿恢复；取消时不重置攻击等其他动作的帧号。参考格号、阶段和绘制握点保存在各 cel 的数据中，源站姿与跑步文件保留。

## 新版持戟蹲姿与蹲走 v2（动画稿）

`haiwang_trident_crouch-v2.aseprite` 为 32×32、9 帧，仅含海王，使用 `绿色裤靴`、`金色鳞甲与颈部`、`后侧手臂`、`金发与脸部`、`金色三叉戟`、`持戟手臂` 六个主体图层，以及用户新增的握点修补层。第 1 帧属于“蹲持”标签，第 2–9 帧属于“蹲走”标签；源稿每帧 80 毫秒，选择“蹲走”标签可循环播放八帧，一轮 640 毫秒。导入身体时使用四个身体图层，武器则按用户当前顺序合成持戟手臂、三叉戟和新增修补层。

初稿头发、脸部和配色取自当时 `haiwang_trident_jump-v2.aseprite` 第 18 帧；头部像素保持不变，向下平移 3–4 像素，步间起伏 1 像素。鳞甲保留原有配色与纹理，压低腰部；裤靴按兰博屈膝、抬脚和换脚轮廓调整，采用海王绿裤靴配色。裤靴参考相对官方图格上移 1 像素、左移 1 像素，脚底基准为 `y=29`。后续用户微调以当前工程像素为准。

后侧手臂贴身，蹲走的手臂与握点变化幅度均为 1 像素。前臂随握点连接，手掌始终盖住戟杆。静止蹲持前倾 16°，蹲走在 21°–26° 之间随步伐回摆；握点下方戟尾相对站姿源稿收短 4 像素并保留尾饰，三个戟尖均在画布内。

下表记录初稿的倾角与握点，最新位置和轮廓以用户微调工程为准。工程帧号从 1 开始，原版身体格号从 0 开始；握点坐标以画布左上角为 `(0, 0)`：

| 工程帧 | 标签／蹲走序号 | 原版身体参考格号 | 前倾角 | 握点 `(x, y)` |
| --- | --- | --- | --- | --- |
| 1 | 蹲持 | 6 | 16° | (21.5, 25.5) |
| 2 | 蹲走 1 | 40 | 22° | (20.5, 25.5) |
| 3 | 蹲走 2 | 41 | 24° | (19.5, 25.5) |
| 4 | 蹲走 3 | 42 | 26° | (19.5, 25.5) |
| 5 | 蹲走 4 | 43 | 25° | (19.5, 25.5) |
| 6 | 蹲走 5 | 44 | 23° | (19.5, 25.5) |
| 7 | 蹲走 6 | 45 | 22° | (20.5, 25.5) |
| 8 | 蹲走 7 | 46 | 21° | (20.5, 24.5) |
| 9 | 蹲走 8 | 47 | 22° | (20.5, 24.5) |

参考位置已按 `AnimateActualIdleDuckingFrames()` 与 `AnimateActualDuckingRunning()` 核对，分别为身体格 6、40–47。原版新蹲走分支设置的帧间隔是 25 毫秒；本工程的 80 毫秒用于编辑预览，不会自动改变游戏播放速度。原版参考来自 `Broforce_src/GameAssets/hero/RAMBO_anim_1024x512.png`，同时查看了配套武器贴图中的手臂与枪；交付工程仅保留海王。

初稿检查确认九帧头部与当时源站姿逐像素一致（仅位置改变）、手掌与戟杆重叠连接、八帧蹲走各不相同且首尾可循环，并补齐戟头旋转、腋下、颈部和交叉脚部的单像素接缝。本次用户微调后，九帧合成结果已与实际游戏图集逐像素核对，新增握点修补层完整保留。

2026-09-14 再次同步使用 21:53:46 保存的蹲姿源稿，按七个可见图层的当前顺序重新导出九帧。身体格 6、40–47 与武器格 64–72 均已更新，九组配对逐像素匹配源稿，其余图集格保持原样。DLL 和配套贴图重新构建部署成功，文件校验一致；源文件内容保留，游戏内表现待用户重启后确认。

身体图集 6、40–47 格与扩展武器图集 64–72 格已按源稿同步完成，两个 PNG 已导出。`Start()` 开启 `useNewDuckingFrames`，使用原版八帧蹲走及其 25 毫秒帧间隔；`RenderTrident()` 按实际身体格配对普通持戟，并取消配对帧上的 `SetGunPosition(2f, -1f)` 附加位移。蹲姿蓄力、投掷和近战继续使用各自的攻击姿态。后续微调三叉戟时，应同步同帧持戟手臂、握点及对应图集；参考格号和绘制参数保存在 cel 数据中。

## 地形动作 v2（贴墙、悬挂、攀爬、爬梯、滑索）

`haiwang_trident_traversal-v2.aseprite` 使用 `绿色裤靴`、`金色鳞甲与颈部`、`抓附手臂`、`金发与脸部`、`金色三叉戟`、`活动手臂` 六层。2026-09-15 按用户要求，前 68 帧的三叉戟层清空，双臂完整导出到身体图集；滑索 69–86 帧继续持戟。现有头部、配色、身体与手臂姿势保留，进梯首帧收拢包杆指尖。

| 标签／源稿帧 | 内容 | 游戏身体格 | 源稿预览时长 |
| --- | --- | --- | --- |
| 贴墙 1–10 | 两组探手、伸展、抓附、承重、停驻 | 76–80、86–90 | 67 毫秒／帧 |
| 悬挂 11–28 | 十二帧横移换手、六帧单手停驻收势 | 107–124 | 横移 67、收势 45 毫秒 |
| 攀爬 29–48 | 两组抓附、抬身、换脚和下一抓预备 | 76–95 | 67 毫秒／帧 |
| 爬梯 49–68 | 上行八帧、下滑三帧、停驻三帧、进出梯六帧 | 160–173、192–197 | 上行／停驻 80、下滑 67、过渡 50 毫秒 |
| 滑索 69–86 | 十二帧逆行换手、六帧滑行收势；身体后仰 | 512–529 | 换手 67、收势 75 毫秒 |

标签包含多个状态段。原版会按状态循环、钳制末帧或按高度选帧，游戏不会直接按 Aseprite 的整段时间轴顺序播放。五个 GIF 用于查看源稿，不能代表全部运行时切换条件。

原版核对依据为本机实际安装的 `Assembly-CSharp.dll` 和 `BroMakerLib.dll` 的反编译结果，DLL 哈希、格号和抓附坐标保存在 [native-animation-map.json](traversal-v2/native-animation-map.json)。本机仓库暂未包含 `Broforce-Source/!SOURCE_ANALYSIS.md`，因此直接核对已安装 DLL。

| 原版方法 | 实际取帧和接入方式 |
| --- | --- |
| `AnimateWallDrag()` | `useNewKnifeClimbingFrames` 开启时，按 `knifeHand % 2` 取 76–80 或 86–90；高速上升时钳制到各组前两帧 |
| `AnimateWallClimb()` / `AnimateWallAnticipation()` | 接触段共用上述格，抬身及预抓附继续取 80–85 或 90–95；保留原版帧计数、攀升速度和按高度选择的逻辑 |
| `AnimateHanging()` | 横移取 107–118，66.7 毫秒／帧；停止后取 119–124，45 毫秒／帧并钳制末帧；保留身体 `(0,-2)` 偏移 |
| `AnimateClimbingLadder()` | 上行 160–167、下滑 168–170、停驻 171–173；原版播放时序保留。持火或侧向分支原本回退到旧 0–3 格，仍贴近梯子时改用 171–173 空手停驻 |
| `AnimateClimbingLadderTransition()` | 192–197；进入每步 25 毫秒，退出每步 33 毫秒，保留原版正反向过渡计数 |
| `AnimateZipline()` | 原取 107–118 换手、119–124 滑行收势；先运行原版，再重定向为 512–529，保留滑行、血粒子／火花及 75 毫秒收势时序 |

本机 BroMaker 的 `BetterAnimation=true` 已开启 `useNewFrames`、`useNewKnifeClimbingFrames`、`useNewLedgeGrappleFrames` 等，但没有设置新版爬梯及进出梯开关。`Start()` 现明确设置 `useNewKnifeClimbingFrames=true`、`useNewLadderClimbingFrames=true`、`useLadderClimbingTransition=true`；保留已有 `useNewDuckingFrames=true`。悬挂和滑索没有独立的 `useNew*Frames` 开关。

登沿的主体分发路径是 `ChangeFrame()` 中的 `ledgeGrapple → AnimateRunning()`，继续接现有跑步；不能仅因 `useNewLedgeGrappleFrames=true` 就把图集 76–89 认作本机常规登沿的独立时序。蹬墙翻转的原 203–214 格及现有跑跳、落地衔接均保留。

悬挂按原版轮廓统一上移 1 像素，横移掌心中心为 `x=22.5,22.5,21.5,18.5,14.5,10.5`，重复两组，`y=8.5`；停驻掌心中心为 `(20.5,8.5)`。滑索继续使用原有 `x=22,22,21,18,14,10`、`y=9` 及停驻 `(20,9)` 的描点参数，保留握杆与倾角变化。抓附点、活动手可见接缝中心、手掌点及参考格保存在 cel 数据与 [traversal-manifest.csv](traversal-v2/traversal-manifest.csv)；被遮住的活动手以 `0,0` 标记。

微调时需要保持两项共用关系：贴墙 1–5 与攀爬 29–33、贴墙 6–10 与攀爬 39–43 共用游戏格，修改后须同步对应帧；进出梯第 63 帧参考最新站姿的身体与双臂，去掉戟并收拢指尖，第 68 帧匹配上行第 49 帧。同步脚本会拒绝像素不同的共用格。

`traversal-v2/sync_traversal.lua` 通过 Aseprite 读取当前主稿，在临时目录生成图集、PNG 和逐帧映射；默认目录为 `%TEMP%/haiwang-unarmed-traversal/`，可用脚本参数 `work` 指定。前 68 帧的三叉戟层必须为空，活动手臂合入身体；仅滑索生成配对武器。`check_traversal.py` 是前次“持戟改空手”的专项检查，包含与当时备份保持身体一致的断言，不适用于本轮重画。`create_traversal.lua` 是历史持戟初稿生成脚本，不用于覆盖当前空手主稿。

### 2026-09-15：按 Rambro 动作像素修正比例

三份独立稿继续使用 32×32 画布、六个可编辑图层和原帧时长，分别对应总稿 1–10、11–28、29–48 帧。贴墙取原版 76–80、86–90，悬挂取 107–124，攀爬取 76–95；10 个共用姿势保持逐像素一致。

双臂取原版可见皮肤轮廓，保留抬手、屈肘、换手、贴身收手以及手臂经过脸前的遮挡。躯干和裤靴沿用原版动作轮廓，整体对齐偏移为 `(0,-1)`；肤色、金发、橙金鳞甲和绿色裤靴采用当前 `haiwang_trident_held-v2.aseprite`。头部按面部位置定位，并随第二轮横移和攀爬转为侧后视角。原版刀刃、刀柄、挥刀残影和红头带不进入海王素材；头带遮住的少量腕臂像素补回皮肤，另补齐 3 处金发接缝。

脸部与弯曲手臂之间保留原版同坐标已有的透明留白，不能把这些留白一律当作漏像素填实。所有新手臂像素均位于原版皮肤或被头带遮挡的位置；没有用任意肩点、肘点和握点连线拉长手臂。

本轮同步总稿前 48 帧和身体格 76–95、107–124，共 38 格，保存后的 Aseprite 图集与 `_Mod/sprite.png` 一致。总稿 49–86 帧的 228 个 cel、图集其余 986 格及隐藏参考层保留；已有站跑跳蹲、爬梯、滑索源稿、武器图集和 DLL 保留。

检查与预览见 [逐帧对照](traversal-v2/rambro-refit-preview.png)、[贴墙并排动画](traversal-v2/wall-rambro-compare.gif)、[悬挂并排动画](traversal-v2/hanging-rambro-compare.gif)、[攀爬并排动画](traversal-v2/climbing-rambro-compare.gif) 和 [素材检查记录](traversal-v2/rambro-refit-checks.json)。本轮只更新素材与本地图集，尚未重新构建、部署或进行游戏实测。

`refit_rambro.lua` 记录本轮 Aseprite MCP 重画过程，只输出临时修订稿；后续手动微调仍以三份独立文件为准。修改前备份为 `../backups/before-rambro-proportions-20260915-064442/`。

## 图集与代码对应

身体图集 `_Mod/sprite.png` 和武器图集 `_Mod/gunSprite.png` 均为 1024×1024，单格 32×32。原武器 0–72 格保留；四类空手动作原占的武器 73–594 格已清空，滑索继续使用 595–756 格。身体额外行用于独立滑索 512–529。

| 场景 | PNG 行号 | 起始列 |
| --- | --- | --- |
| 站立、普通空中动作、近战 | 0 | 0 |
| 跑步 | 0 | 16 |
| 蹲姿 | 1 | 0 |
| 旧单手组，作为未配对动作的回退 | 1 | 16 |

| 持戟动作 | 身体格号 | 武器格号（按帧顺序） |
| --- | --- | --- |
| 站立 | 0 | 9 |
| 跑步、冲刺 | 32–39、96–103 | 10–15、25–26 |
| 蹲持 | 6 | 64 |
| 蹲走 | 40–47 | 65–72 |
| 前进跳跃 | 64–69 | 27–31、41 |
| 原地跳跃 | 70–75 | 42–47 |
| 落地接跑步 | 48–50 | 57–59 |
| 落地接站立 | 104–106 | 60–62 |

站立、跑步、跳跃和落地继续使用原图集的 27 个配对格，编号公式为 `9 + 16 * (index / 7) + index % 7`（整数除法）。蹲姿源稿第 1–9 帧另用 `64 + (工程帧号 - 1)`，合计 36 个原有配对武器格。站跑跳蹲的攻击姿态继续使用原组。

| 地形身体格 | 动作索引 | 武器显示与区域 |
| --- | --- | --- |
| 76–95 | 0–19 | 空手；原 73–252 已清空 |
| 107–124 | 20–37 | 空手；原 253–414 已清空 |
| 160–173 | 38–51 | 空手；原 415–540 已清空 |
| 192–197 | 52–57 | 空手；原 541–594 已清空 |
| 512–529 | 58–75 | 595–756 |

仅滑索使用配对公式 `595 + (身体格 - 512) * 9 + TridentPose`，共 162 格。九种姿态依次为持戟、抬手、蓄力、蓄满、离手、收手、突刺准备、突刺、突刺收手。离手格保留活动手臂，收招恢复同一滑索身体格对应的握点；突刺仍前伸 9 像素。详见 [attack-audit.csv](traversal-v2/attack-audit.csv)。

`RenderTrident()` 在四类空手身体格中关闭武器层，持火也不强制显示；双臂属于身体贴图。回到可持武器的普通动作时恢复显示，避免原版先更新武器、后更新身体造成短暂缺失。滑索保留配对武器与攻击姿态，原攻击时序及伤害不变。两张图集的纹理与 32×32 网格在 `Start()` 刷新。

`_Mod/sprite.png`、`_Mod/gunSprite.png` 已由更新后的 Aseprite 图集导出。以后再次微调源稿，仍需同步图集和导出资源；`BuildBro.ps1` 不会读取或转换 `tools` 下的 Aseprite 工程。

后续微调需要保持以下衔接关系，工程帧号从 1 开始，图集格号从 0 开始：

- 修改跑步稿第 1 帧时，同步跳跃稿第 9 帧；对应身体格为 32、96、50，武器格为 10、59，分别用于跑步、冲刺及落地接跑步。
- 修改当前站姿时，以本目录 `haiwang_trident_held-v2.aseprite` 为主稿，同步跳跃第 18 帧及身体格 0、106、武器格 9、62。进梯第 63 帧／身体格 192 使用空手版本，需单独收拢手指并保持武器格 541–549 为空。

此版动画需要配套的 `Bro Template.dll`、`sprite.png` 和 `gunSprite.png`。导出 PNG 后执行 `BuildBro.ps1` 一并构建部署，重启游戏加载。若改变图集格号或落地时长，还需同步 `src/TridentMovementAnimation.cs` 中的映射和时序；只调整 Aseprite 时间轴不会改变代码中的落地时长。

旧单手回退组的离手格仍为空；滑索配对组的离手格保留活动手臂。空手与持戟动作切换仍需在游戏内确认。

_Mod/projectiles/Trident.png 为 64×32，左格普通、右格蓄力。SpriteSM 的左下像素纵坐标从图片顶边计算，第一行使用 y=32；弹体明确刷新纹理、网格尺寸和偏移，碰撞位置对齐戟尖。

- src/BroTemplate.cs：输入接线、角色动画、突刺、取消逻辑和水墙特技。
- src/TridentAttackState.cs：独立输入时序，与 Unity 无关。
- src/TridentMovementAnimation.cs：空手动作识别、滑索九姿态配对与格号重定向、爬梯停驻衔接、落地时序及取消逻辑。
- src/TridentStrikeGeometry.cs：三叉戟突刺的命中几何计算。
- src/TridentProjectile.cs：普通与蓄力弹体、独立命中记录、飞行与消散。
- src/TridentCollision.cs：三叉戟遮挡检查。
- src/RisingWaveDamage.cs：水墙对骨虫等独立 `DamageRelay` 受伤部件的补充命中。
- 原 TidalBolt 类不再作为角色的主武器生成。

## 验证与构建

以下代码测试和部署结果属于此前接入；本轮比例修正的逐帧检查记录为 [rambro-refit-checks.json](traversal-v2/rambro-refit-checks.json)，未重新执行构建部署。

本轮自动测试通过：攻击 390 次断言、水墙 16 次断言、移动动画 1856 次检查。覆盖四类空手动作不选择武器帧、滑索配对与攻击恢复、爬梯回退和原有落地时序。

素材检查通过：86 帧、六层、五个中文标签完整，身体与武器输出匹配当前源稿；前 68 帧不含三叉戟、双臂完整，无游离像素、半透明像素或小型封闭透明接缝。滑索的 18 帧源稿、身体格及 162 个武器格均与修改前一致；站跑跳蹲五份源稿与武器 0–72 格保留。详见 [asset-checks.json](traversal-v2/asset-checks.json)。

本次使用 `BuildBro.ps1` 构建并部署配套 DLL 与贴图；执行结果、文件哈希和部署目录见 [deployment.json](traversal-v2/deployment.json)。

在项目目录执行：

~~~powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tests/Test-Trident.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File BuildBro.ps1
~~~

BuildBro.ps1 使用 .NET Framework 3.5 编译，随后将 _Mod 部署到 LocalBroforcePath.props 指定的 BroMaker_Storage/Bro Template。增加 UnityEngine.AudioModule.dll 引用，用于投戟音效。

构建与导出检查不能替代游戏内验证。首次进入游戏应确认短按与蓄力、贴墙出手、穿透多个敌人、厚重敌人挡停、空中与蹲姿投掷、近战中断和水墙特技。

本次空手显示修改尚未进行游戏实测。重启加载配套文件后，确认贴墙、悬挂、攀爬和爬梯时空手，滑索持戟，离开这些动作后正常恢复武器。

初次三叉戟实现前的备份：`../backups/before-trident-20260913-211241/`；站跑跳落地 v2 接入前的备份：`../backups/before-v2-animation-20260914-161658/`；本次蹲姿 v2 接入前的备份：`../backups/before-crouch-v2-20260914-210129/`，包含代码、测试、文档、图集、蹲姿源稿、本地运行资源和原部署 DLL、贴图。

上一轮站姿与蹲姿同步前备份：`../backups/before-user-pose-sync-20260914-214023/`，包含当轮用户站姿与蹲姿源稿、同步前的跳跃稿和图集、文档、代码及本地与部署 DLL、贴图。

本次蹲姿再次微调同步前备份：`../backups/before-crouch-refresh-20260914-215745/`，包含本轮蹲姿源稿、同步前图集、文档、本地与部署 DLL 和贴图，以及记录源文件哈希的 `sync-info.json`。

地形动作初次接入前备份：`../backups/before-traversal-v2-20260914-222759/`。本次空手修改前备份：`../backups/before-unarmed-traversal-20260915-000557/`，包含本轮源稿、代码、测试、图集、文档及本地 DLL／贴图。
