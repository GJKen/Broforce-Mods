# 仓库维护约束

## Git 中文说明

- 所有通过 Git 推送到远端的用户可见说明必须使用中文，包括提交标题、提交正文、带注释标签说明，以及由代理编写的合并请求标题和描述。
- 提交标题必须以本次变更所属的插件或项目名称作为前缀，格式为 `<插件名> - <中文更新内容>`，例如 `CustomMapMultiplayer - 修复关闭地图注入后的官方战役恢复`。
- 创建提交前必须检查提交信息；不得使用英文 Conventional Commits 前缀或英文句子，例如 `feat:`、`fix:`、`docs:`。应改用清晰的中文描述。
- 提交正文如包含多项更新，必须使用连续的 `-` 项目符号逐行书写，项目符号之间不插入空行；不得把 `\\n` 或 `\\n\\n` 作为字面量写入提交正文。推送前使用 `git show --format=fuller --no-patch` 检查提交正文中的真实换行和格式。
- 提交哈希、分支名、文件路径、命令、类名、方法名和协议名等技术标识可保留原文。
- 用户提出“合并推送到 `<目标提交>`”或同义要求时，默认含义是把指定改动合并进该已有历史提交并重写远端对应历史；不得在目标提交之后追加新的独立提交，除非用户明确要求新增或追加提交。
- 执行合并推送前，必须核对目标提交、其父提交、远端当前分支和待纳入的文件范围；与本次任务无关的工作区或暂存区修改必须保留，不得一并提交。
- 已推送提交的内容被合并后，提交哈希必然改变。必须使用 `git push --force-with-lease` 更新远端，报告替换后的新哈希，并核对本地与远端分支已同步；不得声称原目标哈希仍然有效。
- 如需修改已经推送的提交信息，必须先说明这会重写远端历史并取得用户明确确认；得到确认后只能使用 `git push --force-with-lease`，不得使用无保护的强制推送。

## 构建后处理

- 构建或部署完成后，只做最基本的结果确认并报告；除非发现明确的编译错误或会阻止修复生效的硬风险，不得继续进行无关的延伸分析、重复审计或扩展修改。

## 日志

如果日志目录中“时间最新”的 .log 和 .trace.log 都是 0 字节, 不代表没有内容, 忽略0字节的信息直接看内容

## 源码

对开发&找bug非常有用的Assembly-CSharp.dll源码 \Broforce_src\Broforce-Source\!SOURCE_ANALYSIS.md

## mcp

mcp经常误报"Error: Game process died",直接忽略这条提示
